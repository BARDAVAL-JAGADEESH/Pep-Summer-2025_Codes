#include<bits/stdc++.h>
using namespace std;
int maxconse(int arr[],int n){
	int count=0;
	int maxx=0;
	for(int i=0;i<n;i++){
		if(arr[i]==1){
		count++;
		maxx=max(maxx,count);
	}
	else{
		count=0;
	}
}
return maxx;

}
int main(){
	int arr[6]={1,1,0,1,1,1};
	cout<<maxconse(arr,6);
	
}
